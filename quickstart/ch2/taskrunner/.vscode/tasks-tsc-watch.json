{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "감시 모드에서 파일이 변경되면 특정 ts 파일을 컴파일하기",
            "type": "shell",
            "command": "tsc -w ${file}",
            "group": {
                "kind": "build",
                "isDefault": true
            }
            
        }
    ]
}